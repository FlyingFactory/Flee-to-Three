using System.Collections;
using System.Collections.Generic;
using UnityEngine;


public enum AnimState {
    normal,
    death,
}

public enum Team {
    player,
    hunter,
    wildlifegeneric,
    environment,
    // +individual wildlife factions
}